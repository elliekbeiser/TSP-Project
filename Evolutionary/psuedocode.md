```
helper function to parse txt
    read distance file and parse into lists for each city
    read name file and create list of names
    returns lists

helper function to calculate distance of route
    for all cities in the routes and back to the first route
        add distances
    return distances

tsp_evolutionary with thirty_cities_names and thirty_cities_dist as parameters
    
    
```
